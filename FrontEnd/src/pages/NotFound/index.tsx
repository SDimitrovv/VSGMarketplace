const NotFound = (): JSX.Element => {
    
    return (
        <section id='errorSection'>
            <h2>Error 404...</h2>
            <p>This page doesn't exist</p>
        </section>
    );
};

export default NotFound;